# Generated by scripts/generate-config — DO NOT edit ngrok.yml by hand.
# Template reference only. See README.md

version: "3"

agent:
  authtoken: ${NGROK_AUTHTOKEN}
  web_addr: ${NGROK_WEB_ADDR:-127.0.0.1:4040}

tunnels:
  frontend:
    proto: http
    addr: 4200
    schemes:
      - https
    basic_auth:
      - "${NGROK_BASIC_AUTH_USER}:${NGROK_BASIC_AUTH_PASS}"
    # Optional reserved domain (paid plan):
    # hostname: ${NGROK_FRONTEND_DOMAIN}

  backend:
    proto: http
    addr: 8080
    schemes:
      - https
    inspect: false
    basic_auth:
      - "${NGROK_BASIC_AUTH_USER}:${NGROK_BASIC_AUTH_PASS}"
    # Optional reserved domain (paid plan):
    # hostname: ${NGROK_BACKEND_DOMAIN}
    # Optional IP allowlist (paid plan) — injected by generate-config when set:
    # ip_restriction:
    #   allow_cidrs:
    #     - 203.0.113.0/24
