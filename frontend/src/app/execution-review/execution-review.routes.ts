import { Routes } from '@angular/router';
import { ExecutionReviewComponent } from './execution-review.component';

export const EXECUTION_REVIEW_ROUTES: Routes = [
  { path: '', component: ExecutionReviewComponent }
];
